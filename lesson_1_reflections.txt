How did viewing a diff between two versions of a file help you see the bug that
was introduced?

    By automating the search process

How could having easy access to the entire history of a file make you a more
efficient programmer in the long term?

    By reducing the time it takes to find errors, especially in collaboration with groups.

What do you think are the pros and cons of manually choosing when to create a
commit, like you do in Git, vs having versions automatically saved, like Google
docs does?

    Pros are commits are morely likely to correspond to meaningful changes. Cons are that changes can be uncommited due to human error/forgetfulness.

Why do you think some version control systems, like Git, allow saving multiple
files in one commit, while others, like Google Docs, treat each file separately?

    Git allows multiple files to be saved due to the necessity of saving all inter-related files in software development whereas Google Docs is not designed specifically for software development and is instead, for general purpose document management.

How can you use the commands git log and git diff to view the history of files?

    Fill in your answer here

How might using version control make you more confident to make changes that
could break something?

    Fill in your answer here

Now that you have your workspace set up, what do you want to try using Git for?

    Fill in your answer here

How did viewing a diff between two versions of a file help you see the bug that was introduced?
	
	This was answered at beginning of file.

What happens when you initialize a repository? Why do you need to do it?

	Git creates the meta file structure that allows it to track the directory's file history.  The init commands tells Git where your new repository is.